{
  "name": "darb",
  "version": "1.0.0",
  "description": "درب - سكرتير خاص فيك",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../web && npm install && cd ../mobile && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:web": "cd web && npm start",
    "start:backend": "cd backend && npm start"
  }
}
